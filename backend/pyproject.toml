[project]
name = "previewer-backend"
version = "0.1.0"
description = "Backend do previewer: domínio, infraestrutura e web"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
