services:
  app:
    build: .
    env_file: .env
    environment:
      STORAGE_ROOT: /data
      DATABASE_URL: postgresql+psycopg://previewer:previewer@db:5432/previewer
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    volumes:
      - storedata:/data

  ngrok:
    image: ngrok/ngrok:latest
    restart: unless-stopped
    env_file: .env
    command: ${NGROK_CMD:-http app:8000 --log stdout}
    ports:
      - "4040:4040"
    depends_on:
      - app

  db:
    image: postgres:16
    environment:
      POSTGRES_USER: previewer
      POSTGRES_PASSWORD: previewer
      POSTGRES_DB: previewer
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U previewer"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  storedata:
  pgdata:
