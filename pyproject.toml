[project]
name = "previewer-domain"
version = "0.1.0"
description = "Domínio puro do previewer: fonte, layout de tela e codec de texto"
requires-python = ">=3.13"

[tool.pytest.ini_options]
testpaths = ["tests"]
